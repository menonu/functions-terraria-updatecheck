# functions-terraria-updatecheck

## Setup

You need to set these environment variables before use.

'MONGODB_URL' is the CONNECTION STRING of Azure Cosmos DB(MongoDB).
'SLACK_WEBHOOK_URL' is the Slack incoming webhook URL.